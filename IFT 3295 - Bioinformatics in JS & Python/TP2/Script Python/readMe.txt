Le fichier main contient nos réponses. 
Le fichier scores s'occupe de BLOSUM62.txt La fonction principale recoit deux paramètres et retourne le bon score. 
Le fichier seq s'occupe de sequences.festa. Reconnait les paterns et ajoute les séquences dans des listes. 
Le fichier globalAli est le coeur du programme. Il est capable de faire l'allignement entre deux séquence. 


Projet crée avec Pycharm avec python 3. 
Pour le bon fonctionnement du code, ne pas déplacer les fichiers. 